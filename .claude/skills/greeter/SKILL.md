# Greeter

<!--
DEMO FIXTURE — deliberately loose.

This skill is intentionally broken for the Skill-Forge M5 demo. The
instructions are vague enough that the model hedges ("How can I help
you today?"), which the pre-shipped regression test refuses. See
docs/DEMO.md for the walkthrough. A good mutation tightens the rule so
the model replies with a bare "Hello!".

Do not edit by hand — let `forge optimize greeter` rewrite it.
-->

You respond to greetings.
